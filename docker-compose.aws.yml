services:
  chess-study:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: chess-study
    restart: unless-stopped
    ports:
      - "80:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
